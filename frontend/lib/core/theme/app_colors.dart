import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary & Emerald Brand Colors (matching the 3D glowing logo)
  static const Color primaryGreen = Color(0xFF00C853); // INFURNUS emerald green
  static const Color emeraldNeon = Color(0xFF00E676); // Glowing neon crystal green from logo
  static const Color emeraldAccent = Color(0xFF00FF88); // High-contrast green highlight
  static const Color emeraldGlow = Color(0x5900E676); // Ambient translucent glow
  static const Color primaryDark = Color(0xFF121212); // Premium dark hero areas

  // Logo & Dark Luxury Backgrounds
  static const Color logoBackground = Color(0xFF000000); // Pitch black matching the logo background
  static const Color darkBackground = Color(0xFF080C09); // Deep dark obsidian surface
  static const Color darkSurface = Color(0xFF101713); // Card/sheet dark surface
  static const Color darkCard = Color(0xFF16211A); // Elevated dark container
  static const Color darkBorder = Color(0x3300E676); // Subtle emerald border
  
  // Neutral Colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFD32F2F);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF121212);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
  static const Color textDarkPrimary = Color(0xFFFFFFFF);
  static const Color textDarkSecondary = Color(0xFFB0BEC5);
  
  // Others
  static const Color cardShadow = Color(0x0A000000);
  static const Color divider = Color(0xFFEEEEEE);
}
